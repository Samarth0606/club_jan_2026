

// //hoisting (normal)
console.log(a); //undefined

var a = true;

//for let => value unavailable instead of undefined
// ------------------------------

//hoisting 

console.log(b); // error: cannot access b before initialization
// let b = true;
const b = true;

// mcp     DTZ     cep
// DTZ: time span between mcp and cep where the variables 
// declareed with let & const are not accessible (err: special error)

